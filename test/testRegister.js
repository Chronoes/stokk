// import {expect} from 'chai';
// import {routes} from '../server/api-router';

describe('Registration', () => {
  describe('endpoint', () => {
    it('should receive a non-empty POST request', () => {
      // console.log(routes);
    });
  });
});
